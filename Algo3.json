FUNCTION algo_3(arr : ARRAY_OF INTEGER) : INTEGER
def insertion_sort(arr):
    for i from 1 to arr.length[i]
        key = arr[i]
        j = i - 1
        while j >= 0 and key < arr[j]:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr